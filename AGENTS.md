# AGENTS.md

## 项目快速恢复入口

项目路径：

```text
E:\work\performance-report-assistant-skill
```

核心 skill 路径：

```text
performance-report-assistant/
```

进入本项目后按顺序读取：

1. `AGENTS.md`
2. `docs/status.md` 顶部 `Current Handoff Snapshot (2026-07-03)`
3. 如需企微采集器边界，再读 `performance-report-assistant/references/wecom-smart-summary-collector.md`
4. 如需报告采访规则，再读 `performance-report-assistant/SKILL.md`

不要恢复以下文件：

```text
CLAUDE.md
AGENT.md
```

用户已手动删除它们，当前唯一根入口是 `AGENTS.md`。

## 全局协作铁律

当用户讨论需求、产品范围、验收标准、任务拆解、项目状态或执行安排时，Codex 默认负责：

1. 理解并复述用户需求，确认目标、范围、约束、风险和验收标准。
2. 将需求转化为专业、可执行、可派发给 Claude 的任务文档。
3. 在需要 Claude 执行时，先沉淀文档，再给用户可直接复制给 Claude 的执行提示词。
4. Claude 完成后，与用户一起验收。

除非用户明确要求 Codex 直接实现、修复或运行某项操作，否则 Codex 不直接改代码，不越过任务文档替 Claude 执行实现工作。

项目协作、执行交接、验收、阻塞点、后续计划必须及时更新 `docs/status.md`。普通问答不主动写文件。

## 当前状态

当前没有新的 Claude 返工任务。

最新完成的是一次完整周报生成闭环：

- 报告类型：周报
- 读者：直属领导
- 周期：本周，已解析为 `2026-06-29 至 2026-07-03`
- 模板：用户贴出的结构仅作为本次格式参考，未固化为全局模板
- git 证据：`outputs/weekly_git_stats_2026-06-29_2026-07-03.md`
- 企微证据：`outputs/wecom_summary_live_2026-06-29_2026-07-03.md` / `.json`
- 企微 run：`outputs/wecom_runs/20260703-110152-77IJ/`
- 已通过周报留档：`outputs/weekly_report_2026-06-29_2026-07-03.md`

用户已确认该周报草稿可直接复制使用，本轮周报闭环完成。

## 当前周期证据规则

每次进入新的报告采访 / 内容整合环境时，必须以用户在本轮采访中给出的目标周期为准。

- 旧窗口、旧采集、旧周报、旧绩效、旧 trace 或旧输出文件，只有在周期与本轮目标周期一致，或用户明确要求沿用时，才能作为当前证据。
- 若旧材料周期与本轮目标周期不一致，自动排除为当前证据；不要反复询问用户是否使用明显不匹配的旧材料。
- 不匹配的旧材料最多只能作为历史背景、流程验证记录或格式/结构参考，不能作为本轮工作事实、统计数字或报告依据。
- 只有当材料本身没有可判断周期，或用户明确说“沿用之前采集结果 / 用上次材料”时，才需要追问材料是否适用当前周期。

## 模板记忆规则

这个 skill 面向多人、多种报告模板。

- 用户提供的模板、示例、已通过草稿，默认只作为本次任务参考。
- 不自动固化为全局模板，也不自动影响其他用户。
- 交付通过后，如果确有复用价值，可以问一次：

```text
是否把这套结构记录为你后续的【周报/绩效/述职】模板？如果不需要，我只把它作为本次参考。
```

- 只有用户明确同意，才记录为可复用模板。
- 记录时必须带适用范围、报告类型、结构规则和限制，不能当成所有人的默认模板。

## git 证据规则

- 如果用户已经在同一轮中明确给出仓库、时间范围、分支范围和作者过滤，且操作只是只读 git 统计 / 临时 clone / 写入统计输出文件，则视为已给出统计范围确认；Codex 应直接执行，不要机械要求用户再回复固定口令。
- 远程 git URL 访问超时或不可用时，Codex 可以只读查找本机同名或同源仓库；只有当本地仓库的 `origin` 与用户提供 URL 匹配时，才可作为替代证据来源，并在报告中说明“远程超时，使用同源本地副本统计”。
- 如果仓库范围、分支范围、作者过滤或时间范围缺失，则先问清楚再统计。

## 企微采集规则

企业微信智能总结自动化只能在用户明确要求并监督授权后执行。

当前企微采集器已经完成多轮受监督实机验证，但仍只能描述为“受监督自动化”，不要宣传为无人值守、跨环境稳定或适配所有企微 UI。

允许：

- 恢复/置前/最大化企业微信窗口
- 截图、区域 OCR、模板匹配
- Interception 输入
- 在 prompt 中加入本次采集指纹
- 点击已验证目标
- 读取剪贴板并校验指纹

禁止：

- 自动发送、删除、编辑或转发消息
- 未确认企业微信前台时继续操作
- 未确认页面状态时点击、粘贴或复制
- 右键菜单复制
- 未知区域 `Ctrl+A/Ctrl+C`
- 多固定坐标试探
- 沿左侧菜单纵向扫描

任何未来全自动企微实机测试都必须由用户再次监督并明确授权。

## 本轮减重结果

2026-07-03 已完成项目减重：

- 删除旧 `docs/archive/tasks/` 历史任务。
- 删除 `docs/tasks/` 中已完成且不再作为入口的历史任务。
- 删除旧企微 run、旧 probe、旧 Excel 验证输出和旧报告输出。
- 保留当前周报证据和当前企微 run：`outputs/wecom_runs/20260703-110152-77IJ/`。
- 将 `AGENTS.md` 与 `docs/status.md` 改为短版恢复入口。

## 当前保留的关键输出

```text
outputs/weekly_report_2026-06-29_2026-07-03.md
outputs/weekly_git_stats_2026-06-29_2026-07-03.md
outputs/wecom_summary_live_2026-06-29_2026-07-03.md
outputs/wecom_summary_live_2026-06-29_2026-07-03.json
outputs/wecom_runs/20260703-110152-77IJ/
```

## 下一步建议

当前可以直接切换窗口。下一个窗口如果继续：

- 若是继续周报：本轮已完成，无需再跑采集或 git 统计。
- 若是新周期报告：重新采访周期、读者、模板、输出位置和证据来源；旧材料按周期规则自动筛选。
- 若是企微采集器维护：先读 `performance-report-assistant/references/wecom-smart-summary-collector.md`，不要直接运行实机测试，除非用户再次监督并明确授权。
