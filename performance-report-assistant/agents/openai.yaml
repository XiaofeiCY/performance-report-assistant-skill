interface:
  display_name: "Performance Report Assistant"
  short_description: "Fill work reports from notes, repository metrics, and templates."
  default_prompt: "Use $performance-report-assistant to turn my weekly reports, repository commits, and Excel template into a monthly performance report."

policy:
  allow_implicit_invocation: true
