interface:
  display_name: "Performance Report Assistant"
  short_description: "Fill work reports from notes, repository metrics, and fixed templates."
  default_prompt: "Use $performance-report-assistant to turn my notes, repository commits, and report template into a polished Chinese work report."

policy:
  allow_implicit_invocation: true
